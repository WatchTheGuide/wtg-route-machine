# E2E Testing Environment Variables (Example)

# Vite Dev Server
VITE_DEV_SERVER_URL=http://localhost:5174

# API Server
VITE_API_URL=http://localhost:3000

# Mock Admin Credentials (for E2E tests only)
CYPRESS_ADMIN_EMAIL=admin@example.com
CYPRESS_ADMIN_PASSWORD=change-me-in-env-test

# Instructions:
# 1. Copy this file to .env.test: cp .env.test.example .env.test
# 2. Update credentials with mock values (no real credentials!)
# 3. .env.test is ignored by git for security
